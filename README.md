@author 巷北  
@time 2025.12.14 10:22  
# 简介

这部分呢, 打算是用`C`, 写好底层的基本数据结构, 然后呢, 在的语言中调用. 关于怎么调用呢, 打算是编译后调用. 比如`python`, 底层的一些数据结构封装库, 肯定是用`C`写的, 这样运行效率更高. 为什么这么做呢? 我觉得, 其它语言的算法运行的效率, 肯定是比`C`低很多的, 而调用`C`, 相对快一些. 而且, 还能统一接口, 其它语言创建好基本结构, 然后调用即可. 最关键的一点, 明白各个语言之间的交互. 脚本之间的导入, 相对而言, 是简单的,, 用一些库就能解决(主要是脚本都在同一个项目中, 方便查找), 而编译后的导入, 没有干过(虽然跨语言也没尝试过, 但我自己默认觉得, 编译后导包更加困难).
感觉很困难, 但是编写过底层的`os`后感觉, 只要让我能运行`hello world`, 其实问题就解决了. 当然, 这一切的准备, 都是在为后续`C++`编写自己的渲染引擎而做准备的. 假如成功了, 我可以将底层封装编译, 用`python`端也能像写`manim`脚本那样, 编写可视化动画. 
动态脚本的添加, 是怎么弄的呢? 对于`python`解释性语言来说, 可以直接嵌入, 但是, 对于`C++`这种编译型语言, 我编译好代码后, 如何实现嵌入呢? 
