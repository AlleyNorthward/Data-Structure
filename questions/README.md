开辟`question`的目的是什么呢? 肯定就是为了解决问题的. 当然, 解决问题, 就必然逃不了数据结构. 所以, 如果这里再跟外界联系的话, 会产生极强的耦合性. 为了更具有一般性呢, 我这里会在`main`中写好, 然后直接将`main`文件复制过来. 这样不会破坏外界库的独立性, 可以灵活移植, 而且测试的代码还能够保留, 后续使用的话也只需要将该代码拿到外面去即可.  

另外, 也可以将这里抽象成一个`debug`库. 那么对谁`debug`呢? 肯定是对`data_structure`. 所以不可避免地导入它们. 产生的问题是, 根目录并不同级, 编译时可能会产生问题.  

想了想, 我是害怕什么呢? 需要额外写`CMakeLists.txt`, 且担心造成结构混乱. 可是, 如果后续开发, 理解这种复杂结构是必不可少的. 所以, 我觉得还是需要尝试一下, 一味地回避, 肯定不行的.  

结构上呢, 我就打算新增`question`文件夹, 内部有需要选择一下`sqlist`文件夹, `sqlist`内部有`src include`. 这样会多出来两个`CMakeLists.txt`文件. 由于`sqlist`内部文件并不需要全部编译, 所以不需要全部添加进库. 这样的话, 每次编译时, 都需要选择哪个文件编译. 另外, 由于需要导入`data_structure`库, 所以需要在内部的`CMakeLists.txt`选择添加`data_structure`库. 这样根目录下并不需要选择添加`data_structure`库了. 不过呢, 外部的`main.c`函数, 还是想要起到测试的作用, 所以需要选择一下. 不过这个选择呢, 是手动化的. 能否自动化选择呢? 

一开始想了想, 是否需要单独编译`sqlist`. 这样存在的问题是, 编译时文件夹外部还有`data_structure`, 存在很大的隐患. 最安全的做法就是以最外层文件为根目录编译(当然, 也可以将`data_structure`或者其静态文件放入`sqlist`, 就像`cxx_version`一样. 但是我并不期望`questions`移植出去, 而`cxx_version`是存在移植出去的可能性的, 所以才将静态文件放入的, 目的不同). 

--- 

@author 巷北  
@time 2025.12.11 14:48  

果然, 一切都需要尝试, 才能有理解, 不敢尝试的话, 狗屁不是. 经过上面的操作呢, 又进一步加深了理解. 我上面的想法, 是完全完全错误的. 我认为, 一个库如果依赖另一个库的话, 那么`CMakeLists.txt`就需要导入对应的库, 并且根目录中不用再重复导入. 这是错误的想法. 每一个库, 不论是否依赖于其它库, 其本身就是独立存在的, 不需要导入其他库. 我们需要做的是什么呢? 链接一下依赖库就可以了. 如果都是`PUBLIC`的话, 根目录下, 是需要链接该库, 不用重复链接那个库, 就行了. 这样看下来就非常清晰了, 不用考虑模块间的依附关系, 本目录下的`CMakeLists.txt`, 只需要关注本目录下的文件即可. 

